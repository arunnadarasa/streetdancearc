# README update plan

Refresh `README.md` to reflect the features shipped since the last edit.

## Scope

1. **Move registry / Receipt history**
   - Document the `ReceiptHistoryPanel` on `/moves`.
   - Mention chunked log sweep, ArcScan links, success/failure status, and CID classification (mint, A2H payout, offer claim, nanopayment batch).

2. **Metadata preview step**
   - Document the Step 1 preview in the move form: JSON fields, resulting IPFS CID, confirmation before logging.

3. **Primer route & interactive glossary**
   - Add `/primer` to the routes table.
   - Describe the dancer-friendly concept cards and the tappable glossary with dance analogies.

4. **FX price widget**
   - Mention the footer widget showing last-updated time and CoinGecko mode (demo / pro / none).

5. **Live total calculator**
   - Document real-time conversion in product page, cart drawer, and mint form.

6. **CoinGecko demo key support**
   - Update the FX section to note demo keys (`CG-`) route to the public endpoint with `x-cg-demo-api-key`.

7. **Housekeeping**
   - Keep existing structure and tone.
   - Ensure all secret names and contract addresses stay accurate.
